@model AlbumViewModel

@{
    ViewData["Title"] = "ViewAlbum";
}

<div class="col-sm-4 album-container">
    <input value="@Model.Id" class="album-id" type="hidden" />
    <input value="@Model.Name" class="album-name" type="hidden" />
    <figure class="media-body">
        <button class="container btn show-gallery">
            <img class="album-cover" src="@Url.Content(Model.CoverFilePath)">
        </button>
        <div class="card-body">
            <h3>@Model.Name</h3>
            <div class="d-flex justify-content-between align-items-center">
                <small class="text-muted float-right">@Model.NrOfImages Photos</small>
                <div class="btn-group-xs">
                    <button class="btn btn-xs edit" data-title="Edit" data-toggle="modal" data-target="#modal-edit-form">
                        <span class="fa fa-pencil">
                        </span>
                    </button>
                    <button class="btn btn-xs delete" data-title="Delete" data-toggle="modal" data-target="#delete">
                        <span class="fa fa-trash"></span>
                    </button>
                </div>
            </div>
        </div>
    </figure>

    @*<div class="modal fade gallery-modal" role="dialog">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <a class="close" data-dismiss="modal">&times;</a>
                    <h4 class="modal-title">@Model.Name</h4>
                </div>
                <div class="modal-body">

                    <div class="carousel slide gallery-carousel" data-ride="carousel">

                        <div class="carousel-inner" style=" width:auto; height: 100% !important;">
                            @for (var i = 0; i < Model.Media.Count; i++)
                            {
                                <div class="item">
                                    <div class="numbertext">@(i + 1) / @Model.Media.Count</div>
                                    <img class="center-block" src="@Url.Action("Render", "Media", new { id = @Model.Media[i].Id })" style="width:auto; height:100%">
                                    <div class="text">@Model.Media[i].Caption</div>
                                </div>
                            }
                        </div>
                        <a class="left carousel-control" href=".gallery-carousel" data-slide="prev">
                            <span class="glyphicon glyphicon-chevron-left"></span>
                            <span class="sr-only">Previous</span>
                        </a>
                        <a class="right carousel-control" href=".gallery-carousel" data-slide="next">
                            <span class="glyphicon glyphicon-chevron-right"></span>
                            <span class="sr-only">Next</span>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>*@
</div>

@*<script src="~/lib/jquery/dist/jquery.js"></script>*@